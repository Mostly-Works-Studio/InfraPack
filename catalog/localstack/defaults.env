# 4.14 is the last version that runs without a LocalStack account/token.
# For a newer image, set LOCALSTACK_VERSION=latest and LOCALSTACK_AUTH_TOKEN.
LOCALSTACK_VERSION=4.14
LOCALSTACK_PORT=4566
# comma-separated list to limit what starts (empty = everything)
LOCALSTACK_SERVICES=
LOCALSTACK_AUTH_TOKEN=
